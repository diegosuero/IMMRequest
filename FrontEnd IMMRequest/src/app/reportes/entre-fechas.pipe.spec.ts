import { EntreFechasPipe } from './entre-fechas.pipe';

describe('EntreFechasPipe', () => {
  it('create an instance', () => {
    const pipe = new EntreFechasPipe();
    expect(pipe).toBeTruthy();
  });
});
