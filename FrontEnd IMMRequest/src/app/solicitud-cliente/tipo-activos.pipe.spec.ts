import { TipoActivosPipe } from './tipo-activos.pipe';

describe('TipoActivosPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoActivosPipe();
    expect(pipe).toBeTruthy();
  });
});
